import React, { Component } from 'react';
// import { View, Navigator, BackAndroid, Text, Image, TouchableHighlight } from 'react-native';
import {route} from 'comm/router';

let _instanc = route.init(
    {
        aaa: require('./common/a').default,
        bbb: require('./common/b').default,
        ccc: require('./common/c').default
    },
    {
        text: '你现在在a页面'
    }
)

export default _instanc
