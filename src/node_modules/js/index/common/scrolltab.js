
import React from 'react';
import {
  StyleSheet,
  Text,
  View,
  ScrollView,
  TouchableOpacity,
  TouchableHighlight
} from 'react-native';
import ScrollableTabView, { DefaultTabBar, } from 'react-native-scrollable-tab-view';

import blog from '../business/blog'
import {wrender} from 'comp/modules/webrender'  //only webview


/*  sample 1 */
export let simpleScrollTab = React.createClass({
  render() {
    return (
      <ScrollableTabView
          renderTabBar={()=><DefaultTabBar backgroundColor='rgba(255, 255, 255, 0.7)' />}
      >
          <View tabLabel='博客' style={{flex:1}}>
            {blog}
          </View>
          <ScrollView tabLabel='RN'>
            {wrender({url: Config.domain+'?tag=RN'})}
          </ScrollView>
          <ScrollView tabLabel='FKP'>
            {wrender({url: Config.domain+'?tag=fkp'})}
          </ScrollView>
      </ScrollableTabView>
    );
  }
});
