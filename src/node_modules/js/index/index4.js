import React, { Component } from 'react';
import {route} from 'comm/router';

Sio(function(socket){
    socket.on('imchat', (msg)=>{
        console.log(msg);
    })
})

// console.log(Sio);
export default class start extends Component {
    render(){
        return (
            route.init( {
                tabbar: require('./index4/tabbar').default,
                web: require('comp/modules/webrender').default,
            })
            .start('tabbar')
        )
    }
}
