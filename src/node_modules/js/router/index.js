import React, { Component } from 'react';
import {route} from 'comm/router';


export default class routerIndex extends Component{
    render() {
        return route.init({
            aaa: require('./business/a').default,
            bbb: require('./business/b').default,
            ccc: require('./business/c').default
        },
        {
            text: '你现在在a页面'
        })
    }
}
